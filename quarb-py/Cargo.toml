[package]
name = "quarb-py"
# The PyPI release mirrors the engine release, same number —
# `pip install quarb` 0.9.0 is engine 0.9.0 (the earlier
# decoupled 0.3.x–0.6.x cadence confused version comparisons).
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
authors.workspace = true
description = "Python bindings for the Quarb query engine — structure-aware queries over JSON, YAML, TOML, CSV, XML, HTML, and Markdown."
readme = "README.md"
# Not a crates.io publication target — this crate ships to PyPI via
# maturin (see pyproject.toml), not to crates.io.
publish = false

[lib]
name = "_quarb"
crate-type = ["cdylib"]

# metatheca's storage layer (apotheca) is unix-only file
# locking, so the mt: target ships on unix wheels and compiles
# out on Windows — the same split the full CLI makes.
[target.'cfg(unix)'.dependencies]
quarb-metatheca = { path = "../quarb-metatheca", version = "0.28.0" }

[dependencies]
quarb = { path = "../quarb", version = "0.28.0" }
quarb-json = { path = "../quarb-json", version = "0.28.0" }
quarb-age = { path = "../quarb-age", version = "0.28.0" }
quarb-arangodb = { path = "../quarb-arangodb", version = "0.28.0" }
quarb-cosmos = { path = "../quarb-cosmos", version = "0.28.0" }
quarb-datastore = { path = "../quarb-datastore", version = "0.28.0" }
quarb-dynamodb = { path = "../quarb-dynamodb", version = "0.28.0" }
quarb-falkordb = { path = "../quarb-falkordb", version = "0.28.0" }
quarb-firebase = { path = "../quarb-firebase", version = "0.28.0" }
quarb-firestore = { path = "../quarb-firestore", version = "0.28.0" }
quarb-github = { path = "../quarb-github", version = "0.28.0" }
quarb-gitlab = { path = "../quarb-gitlab", version = "0.28.0" }
quarb-gsheet = { path = "../quarb-gsheet", version = "0.28.0" }
quarb-kafka = { path = "../quarb-kafka", version = "0.28.0" }
quarb-kubernetes = { path = "../quarb-kubernetes", version = "0.28.0" }
quarb-ldap = { path = "../quarb-ldap", version = "0.28.0" }
quarb-maildir = { path = "../quarb-maildir", version = "0.28.0" }
quarb-memgraph = { path = "../quarb-memgraph", version = "0.28.0" }
quarb-neo4j = { path = "../quarb-neo4j", version = "0.28.0" }
quarb-neptune = { path = "../quarb-neptune", version = "0.28.0" }
quarb-objstore = { path = "../quarb-objstore", version = "0.28.0" }
quarb-postgres = { path = "../quarb-postgres", version = "0.28.0" }
quarb-redis = { path = "../quarb-redis", version = "0.28.0" }
quarb-serve = { path = "../quarb-serve", version = "0.28.0" }
quarb-sparql = { path = "../quarb-sparql", version = "0.28.0" }
quarb-yaml = { path = "../quarb-yaml", version = "0.28.0" }
quarb-toml = { path = "../quarb-toml", version = "0.28.0" }
quarb-csv = { path = "../quarb-csv", version = "0.28.0" }
quarb-xml = { path = "../quarb-xml", version = "0.28.0" }
quarb-html = { path = "../quarb-html", version = "0.28.0" }
quarb-markdown = { path = "../quarb-markdown", version = "0.28.0" }
quarb-text = { path = "../quarb-text", version = "0.28.0" }
quarb-text-html = { path = "../quarb-text-html", version = "0.28.0" }
quarb-text-markdown = { path = "../quarb-text-markdown", version = "0.28.0" }
quarb-sqlite = { path = "../quarb-sqlite", version = "0.28.0", features = ["bundled"] }
quarb-kaiv = { path = "../quarb-kaiv", version = "0.28.0" }
quarb-fs = { path = "../quarb-fs", version = "0.28.0" }
quarb-git = { path = "../quarb-git", version = "0.28.0" }
quarb-archive = { path = "../quarb-archive", version = "0.28.0" }
quarb-xlsx = { path = "../quarb-xlsx", version = "0.28.0" }
quarb-tree-sitter = { path = "../quarb-tree-sitter", version = "0.28.0" }
quarb-code = { path = "../quarb-code", version = "0.28.0" }
quarb-compose = { path = "../quarb-compose", version = "0.28.0" }
quarb-mount = { path = "../quarb-mount", version = "0.28.0" }
quarb-xpath = { path = "../quarb-xpath", version = "0.28.0" }
quarb-jq = { path = "../quarb-jq", version = "0.28.0" }
quarb-sql = { path = "../quarb-sql", version = "0.28.0" }
pyo3 = { version = "0.28", features = ["extension-module", "abi3-py38"] }
