[package]
name = "quarb-athena"
description = "Amazon Athena adapter for the Quarb query engine — the S3 datalake's query layer on the shared relational model"
keywords = ["query", "athena", "aws", "datalake", "quarb"]
categories = ["api-bindings", "database"]
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
homepage.workspace = true

[dependencies]
quarb = { path = "../quarb", version = "0.15.0" }
quarb-aws = { path = "../quarb-aws", version = "0.15.0" }
quarb-relational = { path = "../quarb-relational", version = "0.15.0" }
serde_json.workspace = true
thiserror.workspace = true
ureq.workspace = true
