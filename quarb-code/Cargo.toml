[package]
name = "quarb-code"
description = "The code level for the Quarb query engine — cross-language code navigation (function names are node names)"
keywords = ["query", "code", "navigation", "ast", "quarb"]
categories = ["development-tools", "parsing"]
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
homepage.workspace = true

[dependencies]
quarb = { path = "../quarb", version = "0.26.0" }
quarb-tree-sitter = { path = "../quarb-tree-sitter", version = "0.26.0" }
thiserror.workspace = true
