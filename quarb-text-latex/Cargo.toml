[package]
name = "quarb-text-latex"
description = "LaTeX producer for the Quarb text level: declared sectioning, the at-callout footnote convention, the honest structural subset"
keywords = ["quarb", "latex", "tex", "text"]
categories = ["parser-implementations"]
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
homepage.workspace = true

[dependencies]
quarb-text = { path = "../quarb-text", version = "0.27.0" }

[dev-dependencies]
quarb = { path = "../quarb", version = "0.27.0" }
