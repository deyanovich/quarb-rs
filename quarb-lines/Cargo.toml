[package]
name = "quarb-lines"
version = "0.22.0"
edition = "2024"
description = "Line-atom adapter for the Quarb query engine — every line of a file as a node"
license = "MIT OR Apache-2.0"
repository = "https://gitlab.com/quarb/engine"

[dependencies]
quarb = { path = "../quarb", version = "0.22.0" }
